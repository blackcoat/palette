/**
 * Spec-style unit tests for Squares on the Board during a game of Palette
 */

require ( 'should' )

describe( 'Square', function() {
	describe( '#is_active', function() {
	})
	describe( '#occupied_by', function() {
	  it ( 'returns a single Piece situated in the Square' )
	})
})
